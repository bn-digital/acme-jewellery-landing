import React from 'react';
import './Header.scss'

const Header = () => {
    return (
        <div className={'header'}>
            <div className="container">
            <h1>{'Ring app'}</h1>

            </div>
        </div>
    );
};

export default Header;
