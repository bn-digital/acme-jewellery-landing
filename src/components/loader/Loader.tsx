import React from 'react';
import './Loader.scss'

const Loader = () => {
    return (
        <div className={'loader'}>
            <p>{'Loading ...'}</p>
            
        </div>
    );
};

export default Loader;
